<nav>
    <div>
        <h1>🎬 CineScope</h1>
    </div>
    <div>
        <ul>
            <li><a href="{{ path('film_index_public') }}">Films</a></li>
            <li></li>
        </ul>
    </div>


    <p>

    </p>

    {% if not app.user %}
        <p>
            <a href="{{ path('app_register') }}">Créer un compte</a>
            <a href="{{ path('app_login') }}">Se connecter</a>
        </p>
    {% else %}
        <p>
            Connecté en tant que {{ app.user.email }}
            <a href="{{ path('app_logout') }}">Se déconnecter</a>
        </p>
    {% endif %}
</nav>
